
A: AppContext

1. Add a migration
    dotnet ef migrations add InitialCreated --context AppDbContext -o Migrations -s ..\Cms.Api

2. Update database
    dotnet ef database update -s ..\Cms.Api --context AppDbContext

