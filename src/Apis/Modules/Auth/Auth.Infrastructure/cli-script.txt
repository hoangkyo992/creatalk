
A: AppContext

1. Add a migration
    dotnet ef migrations add InitialCreated --context AppDbContext -o Migrations -s ..\Auth.Api

2. Update database
    dotnet ef database update -s ..\Auth.Api --context AppDbContext

